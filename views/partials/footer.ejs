<!-- Footer Section - ذيل الصفحة -->
<footer class="bg-gradient-to-t from-black to-gray-900/50 border-t border-gold/20 relative">
    <div class="container mx-auto px-4 py-16">
        <!-- Main Footer Content -->
        <div class="grid lg:grid-cols-4 md:grid-cols-2 gap-8 mb-12">
            <!-- Company Info & Logo -->
            <div class="lg:col-span-1">
                <div class="mb-6">
                    <!-- Logo -->
                    <div class="flex items-center space-x-3 <%= language === 'ar' ? 'space-x-reverse' : '' %> mb-4">
                        <div class="w-12 h-12 bg-gradient-to-br from-gold to-yellow-400 rounded-xl flex items-center justify-center glow-effect">
                            <img src="/images/logo.png" alt="FinClick.AI Logo" class="w-10 h-10 object-contain">
                        </div>
                        <div>
                            <h3 class="text-xl font-bold text-gold">FinClick.AI</h3>
                            <p class="text-gold/60 text-sm">
                                <%= language === 'ar' ? 'التحليل المالي الذكي' : 'Intelligent Financial Analysis' %>
                            </p>
                        </div>
                    </div>
                    <p class="text-gold/70 leading-relaxed text-sm">
                        <%= language === 'ar' ?
                        'منصة ثورية للتحليل المالي الذكي الأكثر تطوراً في العالم. نحن نحول التحليل المالي من عملية معقدة تستغرق أياماً إلى نتائج فورية بضغطة زر واحدة.' :
                        'Revolutionary intelligent financial analysis platform - the most advanced in the world. We transform financial analysis from a complex process that takes days to instant results with one click.'
                        %>
                    </p>
                </div>

                <!-- Social Media Links -->
                <div class="flex space-x-4 <%= language === 'ar' ? 'space-x-reverse' : '' %>">
                    <a href="#" class="w-10 h-10 bg-gold/10 border border-gold/20 rounded-lg flex items-center justify-center text-gold hover:bg-gold hover:text-black smooth-transition">
                        <i class="fab fa-twitter"></i>
                    </a>
                    <a href="#" class="w-10 h-10 bg-gold/10 border border-gold/20 rounded-lg flex items-center justify-center text-gold hover:bg-gold hover:text-black smooth-transition">
                        <i class="fab fa-linkedin-in"></i>
                    </a>
                    <a href="#" class="w-10 h-10 bg-gold/10 border border-gold/20 rounded-lg flex items-center justify-center text-gold hover:bg-gold hover:text-black smooth-transition">
                        <i class="fab fa-facebook-f"></i>
                    </a>
                    <a href="#" class="w-10 h-10 bg-gold/10 border border-gold/20 rounded-lg flex items-center justify-center text-gold hover:bg-gold hover:text-black smooth-transition">
                        <i class="fab fa-youtube"></i>
                    </a>
                </div>
            </div>

            <!-- Quick Links -->
            <div>
                <h4 class="text-lg font-bold text-gold mb-6">
                    <%= language === 'ar' ? 'روابط سريعة' : 'Quick Links' %>
                </h4>
                <ul class="space-y-3">
                    <li>
                        <a href="/" class="text-gold/70 hover:text-gold smooth-transition flex items-center <%= language === 'ar' ? 'space-x-reverse' : '' %> space-x-2">
                            <i class="fas fa-home text-xs"></i>
                            <span><%= language === 'ar' ? 'الرئيسية' : 'Home' %></span>
                        </a>
                    </li>
                    <li>
                        <a href="/dashboard" class="text-gold/70 hover:text-gold smooth-transition flex items-center <%= language === 'ar' ? 'space-x-reverse' : '' %> space-x-2">
                            <i class="fas fa-chart-pie text-xs"></i>
                            <span><%= language === 'ar' ? 'لوحة التحكم' : 'Dashboard' %></span>
                        </a>
                    </li>
                    <li>
                        <a href="/company-info" class="text-gold/70 hover:text-gold smooth-transition flex items-center <%= language === 'ar' ? 'space-x-reverse' : '' %> space-x-2">
                            <i class="fas fa-building text-xs"></i>
                            <span><%= language === 'ar' ? 'عن الشركة' : 'About Company' %></span>
                        </a>
                    </li>
                    <li>
                        <a href="/user-guide" class="text-gold/70 hover:text-gold smooth-transition flex items-center <%= language === 'ar' ? 'space-x-reverse' : '' %> space-x-2">
                            <i class="fas fa-book text-xs"></i>
                            <span><%= language === 'ar' ? 'دليل المستخدم' : 'User Guide' %></span>
                        </a>
                    </li>
                    <li>
                        <a href="/register" class="text-gold/70 hover:text-gold smooth-transition flex items-center <%= language === 'ar' ? 'space-x-reverse' : '' %> space-x-2">
                            <i class="fas fa-user-plus text-xs"></i>
                            <span><%= language === 'ar' ? 'إنشاء حساب' : 'Register' %></span>
                        </a>
                    </li>
                </ul>
            </div>

            <!-- Legal & Policies -->
            <div>
                <h4 class="text-lg font-bold text-gold mb-6">
                    <%= language === 'ar' ? 'السياسات والشروط' : 'Policies & Terms' %>
                </h4>
                <ul class="space-y-3">
                    <li>
                        <a href="/privacy" class="text-gold/70 hover:text-gold smooth-transition flex items-center <%= language === 'ar' ? 'space-x-reverse' : '' %> space-x-2">
                            <i class="fas fa-shield-alt text-xs"></i>
                            <span><%= language === 'ar' ? 'سياسة الخصوصية' : 'Privacy Policy' %></span>
                        </a>
                    </li>
                    <li>
                        <a href="/terms" class="text-gold/70 hover:text-gold smooth-transition flex items-center <%= language === 'ar' ? 'space-x-reverse' : '' %> space-x-2">
                            <i class="fas fa-file-contract text-xs"></i>
                            <span><%= language === 'ar' ? 'شروط الاستخدام' : 'Terms of Use' %></span>
                        </a>
                    </li>
                    <li>
                        <a href="/security-policy" class="text-gold/70 hover:text-gold smooth-transition flex items-center <%= language === 'ar' ? 'space-x-reverse' : '' %> space-x-2">
                            <i class="fas fa-lock text-xs"></i>
                            <span><%= language === 'ar' ? 'سياسة الأمان' : 'Security Policy' %></span>
                        </a>
                    </li>
                    <li>
                        <a href="/payment-policy" class="text-gold/70 hover:text-gold smooth-transition flex items-center <%= language === 'ar' ? 'space-x-reverse' : '' %> space-x-2">
                            <i class="fas fa-credit-card text-xs"></i>
                            <span><%= language === 'ar' ? 'سياسة الدفع' : 'Payment Policy' %></span>
                        </a>
                    </li>
                    <li>
                        <a href="/other-policies" class="text-gold/70 hover:text-gold smooth-transition flex items-center <%= language === 'ar' ? 'space-x-reverse' : '' %> space-x-2">
                            <i class="fas fa-gavel text-xs"></i>
                            <span><%= language === 'ar' ? 'سياسات أخرى' : 'Other Policies' %></span>
                        </a>
                    </li>
                </ul>
            </div>

            <!-- Contact Information -->
            <div>
                <h4 class="text-lg font-bold text-gold mb-6">
                    <%= language === 'ar' ? 'معلومات الاتصال' : 'Contact Information' %>
                </h4>
                <div class="space-y-4">
                    <!-- Address -->
                    <div class="flex items-start <%= language === 'ar' ? 'space-x-reverse' : '' %> space-x-3">
                        <div class="w-8 h-8 bg-gold/10 border border-gold/20 rounded-lg flex items-center justify-center flex-shrink-0 mt-1">
                            <i class="fas fa-map-marker-alt text-gold text-sm"></i>
                        </div>
                        <div>
                            <p class="text-gold/70 text-sm leading-relaxed">
                                <%= language === 'ar' ?
                                'المملكة العربية السعودية\nالرياض - حي العليا\nشارع الملك فهد' :
                                'Kingdom of Saudi Arabia\nRiyadh - Al Olaya District\nKing Fahd Road'
                                %>
                            </p>
                        </div>
                    </div>

                    <!-- Email -->
                    <div class="flex items-center <%= language === 'ar' ? 'space-x-reverse' : '' %> space-x-3">
                        <div class="w-8 h-8 bg-gold/10 border border-gold/20 rounded-lg flex items-center justify-center flex-shrink-0">
                            <i class="fas fa-envelope text-gold text-sm"></i>
                        </div>
                        <div>
                            <a href="mailto:info@finclick.ai" class="text-gold/70 hover:text-gold smooth-transition text-sm">
                                info@finclick.ai
                            </a>
                        </div>
                    </div>

                    <!-- Phone -->
                    <div class="flex items-center <%= language === 'ar' ? 'space-x-reverse' : '' %> space-x-3">
                        <div class="w-8 h-8 bg-gold/10 border border-gold/20 rounded-lg flex items-center justify-center flex-shrink-0">
                            <i class="fas fa-phone text-gold text-sm"></i>
                        </div>
                        <div>
                            <a href="tel:+966500000000" class="text-gold/70 hover:text-gold smooth-transition text-sm" dir="ltr">
                                +966 50 000 0000
                            </a>
                        </div>
                    </div>

                    <!-- WhatsApp -->
                    <div class="flex items-center <%= language === 'ar' ? 'space-x-reverse' : '' %> space-x-3">
                        <div class="w-8 h-8 bg-gold/10 border border-gold/20 rounded-lg flex items-center justify-center flex-shrink-0">
                            <i class="fab fa-whatsapp text-gold text-sm"></i>
                        </div>
                        <div>
                            <a href="https://wa.me/966500000000" class="text-gold/70 hover:text-gold smooth-transition text-sm" dir="ltr">
                                +966 50 000 0000
                            </a>
                        </div>
                    </div>

                    <!-- Support Hours -->
                    <div class="flex items-center <%= language === 'ar' ? 'space-x-reverse' : '' %> space-x-3">
                        <div class="w-8 h-8 bg-gold/10 border border-gold/20 rounded-lg flex items-center justify-center flex-shrink-0">
                            <i class="fas fa-clock text-gold text-sm"></i>
                        </div>
                        <div>
                            <p class="text-gold/70 text-sm">
                                <%= language === 'ar' ? 'الدعم الفني: 24/7' : 'Support: 24/7' %>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Newsletter Subscription -->
        <div class="border-t border-gold/20 pt-12 mb-12">
            <div class="bg-gradient-to-r from-gold/10 to-gold/5 border border-gold/20 rounded-2xl p-8 text-center">
                <h3 class="text-2xl font-bold text-gold mb-4">
                    <%= language === 'ar' ? 'اشترك في نشرتنا الإخبارية' : 'Subscribe to Our Newsletter' %>
                </h3>
                <p class="text-gold/70 mb-6 max-w-2xl mx-auto">
                    <%= language === 'ar' ?
                    'احصل على آخر الأخبار والتحديثات حول منصة FinClick.AI والتطورات الجديدة في عالم التحليل المالي الذكي' :
                    'Get the latest news and updates about FinClick.AI platform and new developments in intelligent financial analysis'
                    %>
                </p>
                <div class="flex flex-col sm:flex-row gap-4 max-w-md mx-auto">
                    <input type="email" placeholder="<%= language === 'ar' ? 'أدخل بريدك الإلكتروني' : 'Enter your email address' %>"
                           class="flex-1 px-4 py-3 bg-black/50 border border-gold/20 rounded-xl text-gold placeholder-gold/50 focus:outline-none focus:border-gold smooth-transition">
                    <button class="px-6 py-3 bg-gradient-to-r from-gold to-yellow-400 text-black font-bold rounded-xl hover:from-yellow-400 hover:to-gold smooth-transition glow-effect">
                        <%= language === 'ar' ? 'اشترك' : 'Subscribe' %>
                    </button>
                </div>
            </div>
        </div>

        <!-- Bottom Footer -->
        <div class="border-t border-gold/20 pt-8">
            <div class="flex flex-col lg:flex-row justify-between items-center space-y-4 lg:space-y-0">
                <!-- Copyright -->
                <div class="text-center lg:text-left">
                    <p class="text-gold/60 text-sm">
                        <%= language === 'ar' ?
                        '© 2024 FinClick.AI. جميع الحقوق محفوظة. تطوير: رزان أحمد توفيق' :
                        '© 2024 FinClick.AI. All rights reserved. Developed by: Razan Ahmed Tawfik'
                        %>
                    </p>
                </div>

                <!-- Language Toggle -->
                <div class="flex items-center space-x-4 <%= language === 'ar' ? 'space-x-reverse' : '' %>">
                    <span class="text-gold/70 text-sm">
                        <%= language === 'ar' ? 'اللغة:' : 'Language:' %>
                    </span>
                    <button onclick="toggleLanguage()" class="flex items-center space-x-2 <%= language === 'ar' ? 'space-x-reverse' : '' %> px-4 py-2 bg-gold/10 border border-gold/20 rounded-lg text-gold hover:bg-gold/20 smooth-transition">
                        <i class="fas fa-globe text-sm"></i>
                        <span class="text-sm">
                            <%= language === 'ar' ? 'العربية' : 'English' %>
                        </span>
                        <i class="fas fa-chevron-down text-xs"></i>
                    </button>
                </div>

                <!-- Back to Top -->
                <button onclick="scrollToTop()" class="flex items-center space-x-2 <%= language === 'ar' ? 'space-x-reverse' : '' %> px-4 py-2 bg-gold/10 border border-gold/20 rounded-lg text-gold hover:bg-gold/20 smooth-transition">
                    <i class="fas fa-arrow-up text-sm"></i>
                    <span class="text-sm">
                        <%= language === 'ar' ? 'العودة للأعلى' : 'Back to Top' %>
                    </span>
                </button>
            </div>
        </div>
    </div>

    <!-- Background Elements -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none">
        <div class="absolute bottom-10 left-10 w-2 h-2 bg-gold/20 rounded-full animate-pulse"></div>
        <div class="absolute bottom-20 right-20 w-1 h-1 bg-gold/30 rounded-full animate-ping"></div>
        <div class="absolute top-10 left-1/4 w-3 h-3 border border-gold/10 rounded-full animate-spin" style="animation-duration: 12s;"></div>
    </div>
</footer>

<!-- Footer JavaScript -->
<script>
// Language toggle function
function toggleLanguage() {
    const currentUrl = window.location.href;
    const urlParams = new URLSearchParams(window.location.search);
    const currentLang = urlParams.get('lang') || 'en';
    const newLang = currentLang === 'ar' ? 'en' : 'ar';

    urlParams.set('lang', newLang);
    window.location.href = `${window.location.pathname}?${urlParams.toString()}`;
}

// Smooth scroll to top
function scrollToTop() {
    window.scrollTo({
        top: 0,
        behavior: 'smooth'
    });
}

// Newsletter subscription
document.addEventListener('DOMContentLoaded', function() {
    const newsletterForm = document.querySelector('.flex.flex-col.sm\\:flex-row.gap-4');
    const emailInput = newsletterForm?.querySelector('input[type="email"]');
    const subscribeBtn = newsletterForm?.querySelector('button');

    if (subscribeBtn && emailInput) {
        subscribeBtn.addEventListener('click', function(e) {
            e.preventDefault();
            const email = emailInput.value.trim();

            if (email && isValidEmail(email)) {
                // Simulate subscription process
                subscribeBtn.textContent = '<%= language === "ar" ? "جاري الاشتراك..." : "Subscribing..." %>';
                subscribeBtn.disabled = true;

                setTimeout(() => {
                    alert('<%= language === "ar" ? "تم الاشتراك بنجاح! شكراً لك." : "Successfully subscribed! Thank you." %>');
                    emailInput.value = '';
                    subscribeBtn.textContent = '<%= language === "ar" ? "اشترك" : "Subscribe" %>';
                    subscribeBtn.disabled = false;
                }, 1500);
            } else {
                alert('<%= language === "ar" ? "يرجى إدخال عنوان بريد إلكتروني صحيح" : "Please enter a valid email address" %>');
            }
        });

        emailInput.addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                subscribeBtn.click();
            }
        });
    }
});

// Email validation function
function isValidEmail(email) {
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    return emailRegex.test(email);
}

// Show/hide back to top button based on scroll position
window.addEventListener('scroll', function() {
    const backToTopBtn = document.querySelector('button[onclick="scrollToTop()"]');
    if (backToTopBtn) {
        if (window.pageYOffset > 300) {
            backToTopBtn.style.opacity = '1';
        } else {
            backToTopBtn.style.opacity = '0.7';
        }
    }
});
</script>