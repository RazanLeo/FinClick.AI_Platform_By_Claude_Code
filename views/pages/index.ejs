<!-- Neural Background Pattern -->
<div class="fixed inset-0 bg-neural-pattern neural-bg pointer-events-none"></div>

<!-- Header -->
<header class="fixed top-0 left-0 right-0 z-50 bg-black/95 backdrop-blur-md border-b border-gold/20">
    <div class="container mx-auto">
        <!-- Global Stock Market Indices -->
        <div class="border-b border-gold/10 py-2 overflow-hidden">
            <div class="whitespace-nowrap animate-scroll text-sm">
                <span class="inline-block mx-6">
                    <span class="text-gold font-semibold">تاسي TASI:</span>
                    <span class="text-green-400 font-medium">12,458.50</span>
                    <span class="text-green-400 text-xs">+0.85%</span>
                </span>
                <span class="inline-block mx-6">
                    <span class="text-gold font-semibold">S&P 500:</span>
                    <span class="text-green-400 font-medium">4,567.23</span>
                    <span class="text-green-400 text-xs">+1.23%</span>
                </span>
                <span class="inline-block mx-6">
                    <span class="text-gold font-semibold">NASDAQ:</span>
                    <span class="text-green-400 font-medium">14,234.56</span>
                    <span class="text-green-400 text-xs">+2.1%</span>
                </span>
                <span class="inline-block mx-6">
                    <span class="text-gold font-semibold">DJI:</span>
                    <span class="text-green-400 font-medium">33,876.21</span>
                    <span class="text-green-400 text-xs">+0.92%</span>
                </span>
                <span class="inline-block mx-6">
                    <span class="text-gold font-semibold">FTSE 100:</span>
                    <span class="text-red-400 font-medium">7,234.12</span>
                    <span class="text-red-400 text-xs">-0.45%</span>
                </span>
                <span class="inline-block mx-6">
                    <span class="text-gold font-semibold">DAX:</span>
                    <span class="text-green-400 font-medium">15,678.90</span>
                    <span class="text-green-400 text-xs">+0.76%</span>
                </span>
                <span class="inline-block mx-6">
                    <span class="text-gold font-semibold">CAC 40:</span>
                    <span class="text-green-400 font-medium">7,156.34</span>
                    <span class="text-green-400 text-xs">+0.58%</span>
                </span>
                <span class="inline-block mx-6">
                    <span class="text-gold font-semibold">Nikkei 225:</span>
                    <span class="text-green-400 font-medium">32,145.67</span>
                    <span class="text-green-400 text-xs">+1.12%</span>
                </span>
                <span class="inline-block mx-6">
                    <span class="text-gold font-semibold">Hang Seng:</span>
                    <span class="text-red-400 font-medium">18,756.43</span>
                    <span class="text-red-400 text-xs">-1.25%</span>
                </span>
                <span class="inline-block mx-6">
                    <span class="text-gold font-semibold">Shanghai:</span>
                    <span class="text-green-400 font-medium">3,234.87</span>
                    <span class="text-green-400 text-xs">+0.67%</span>
                </span>
                <span class="inline-block mx-6">
                    <span class="text-gold font-semibold">BSE Sensex:</span>
                    <span class="text-green-400 font-medium">65,432.18</span>
                    <span class="text-green-400 text-xs">+1.45%</span>
                </span>
                <span class="inline-block mx-6">
                    <span class="text-gold font-semibold">TSX:</span>
                    <span class="text-green-400 font-medium">20,567.89</span>
                    <span class="text-green-400 text-xs">+0.34%</span>
                </span>
                <span class="inline-block mx-6">
                    <span class="text-gold font-semibold">ASX 200:</span>
                    <span class="text-red-400 font-medium">7,089.45</span>
                    <span class="text-red-400 text-xs">-0.23%</span>
                </span>
            </div>
        </div>

        <!-- Main Navigation -->
        <nav class="px-4 py-4">
            <div class="flex items-center justify-between">
                <!-- Logo & Brand -->
                <div class="flex items-center space-x-4 <%= language === 'ar' ? 'space-x-reverse' : '' %>">
                    <div class="w-12 h-12 glow-effect rounded-full flex items-center justify-center">
                        <img src="/images/logo.png" alt="FinClick.AI Logo" class="w-10 h-10 object-contain pulse-gold">
                    </div>
                    <div>
                        <h1 class="text-xl font-bold text-gold">FinClick.AI</h1>
                        <p class="text-xs text-gold/70">
                            <%= language === 'ar' ? 'منصة التحليل المالي الذكية والثورية' : 'Revolutionary Intelligent Financial Analysis Platform' %>
                        </p>
                    </div>
                </div>

                <!-- Search Bar -->
                <div class="hidden md:flex flex-1 max-w-md mx-8">
                    <div class="relative w-full">
                        <input type="text"
                               placeholder="<%= language === 'ar' ? 'ابحث في النظام...' : 'Search the system...' %>"
                               class="w-full px-4 py-2.5 bg-black border border-gold/30 rounded-full text-gold placeholder-gold/50 focus:outline-none focus:border-gold focus:ring-2 focus:ring-gold/20 smooth-transition">
                        <button class="absolute <%= language === 'ar' ? 'left-4' : 'right-4' %> top-1/2 transform -translate-y-1/2 text-gold/70 hover:text-gold smooth-transition glow-effect p-2 rounded-full">
                            <i class="fas fa-search"></i>
                        </button>
                    </div>
                </div>

                <!-- Right Section -->
                <div class="flex items-center space-x-3 <%= language === 'ar' ? 'space-x-reverse' : '' %>">
                    <!-- Language Toggle -->
                    <button onclick="toggleLanguage()" class="flex items-center space-x-2 <%= language === 'ar' ? 'space-x-reverse' : '' %> px-3 py-2 rounded-lg bg-black border border-gold/30 hover:border-gold hover-glow smooth-transition">
                        <img src="/images/flags/<%= language === 'ar' ? 'sa' : 'us' %>.png" alt="<%= language === 'ar' ? 'Saudi Arabia' : 'United States' %>" class="w-4 h-4 rounded-sm">
                        <span class="text-sm font-medium text-gold"><%= language === 'ar' ? 'العربية' : 'English' %></span>
                        <i class="fas fa-globe text-gold/70"></i>
                    </button>

                    <!-- Notifications -->
                    <button class="relative p-3 text-gold/70 hover:text-gold hover-glow rounded-full smooth-transition">
                        <i class="fas fa-bell text-lg"></i>
                        <span class="absolute -top-1 -right-1 w-5 h-5 bg-red-500 text-white text-xs rounded-full flex items-center justify-center pulse-gold">3</span>
                    </button>

                    <!-- Auth Buttons -->
                    <% if (user) { %>
                    <div class="flex items-center space-x-3 <%= language === 'ar' ? 'space-x-reverse' : '' %>">
                        <div class="text-sm">
                            <span class="text-gold/70"><%= language === 'ar' ? 'مرحباً،' : 'Welcome,' %></span>
                            <span class="font-medium text-gold"><%= user.firstName %></span>
                        </div>
                        <a href="/dashboard" class="px-4 py-2 bg-gold text-black rounded-lg font-medium hover:bg-yellow-400 smooth-transition glow-effect">
                            <%= language === 'ar' ? 'لوحة التحكم' : 'Dashboard' %>
                        </a>
                        <a href="/api/auth/logout" class="px-4 py-2 border border-gold/50 text-gold rounded-lg font-medium hover:border-gold hover:bg-gold/10 smooth-transition">
                            <%= language === 'ar' ? 'تسجيل خروج' : 'Logout' %>
                        </a>
                    </div>
                    <% } else { %>
                    <div class="flex items-center space-x-3 <%= language === 'ar' ? 'space-x-reverse' : '' %>">
                        <a href="/login" class="px-4 py-2 border border-gold/50 text-gold rounded-lg font-medium hover:border-gold hover:bg-gold/10 smooth-transition">
                            <%= language === 'ar' ? 'تسجيل الدخول' : 'Login' %>
                        </a>
                        <a href="/register" class="px-4 py-2 bg-gold text-black rounded-lg font-medium hover:bg-yellow-400 smooth-transition pulse-gold">
                            <%= language === 'ar' ? 'إنشاء حساب' : 'Create Account' %>
                        </a>
                    </div>
                    <% } %>
                </div>

                <!-- Mobile Menu Toggle -->
                <button id="mobile-menu-toggle" class="md:hidden p-2 text-gold hover:text-yellow-400 smooth-transition">
                    <i class="fas fa-bars text-xl"></i>
                </button>
            </div>

            <!-- Navigation Menu -->
            <div class="hidden md:flex items-center justify-center mt-4 space-x-8 <%= language === 'ar' ? 'space-x-reverse' : '' %>">
                <a href="#home" class="text-gold/80 hover:text-gold smooth-transition relative group">
                    <i class="fas fa-home <%= language === 'ar' ? 'ml-2' : 'mr-2' %>"></i>
                    <%= language === 'ar' ? 'الرئيسية' : 'Home' %>
                    <div class="absolute -bottom-1 left-0 w-0 h-0.5 bg-gold group-hover:w-full smooth-transition"></div>
                </a>
                <a href="#features" class="text-gold/80 hover:text-gold smooth-transition relative group">
                    <i class="fas fa-star <%= language === 'ar' ? 'ml-2' : 'mr-2' %>"></i>
                    <%= language === 'ar' ? 'مميزات النظام' : 'Features' %>
                    <div class="absolute -bottom-1 left-0 w-0 h-0.5 bg-gold group-hover:w-full smooth-transition"></div>
                </a>
                <a href="#analysis-types" class="text-gold/80 hover:text-gold smooth-transition relative group">
                    <i class="fas fa-chart-line <%= language === 'ar' ? 'ml-2' : 'mr-2' %>"></i>
                    <%= language === 'ar' ? 'أنواع التحليل' : 'Analysis Types' %>
                    <div class="absolute -bottom-1 left-0 w-0 h-0.5 bg-gold group-hover:w-full smooth-transition"></div>
                </a>
                <a href="#free-tools" class="text-gold/80 hover:text-gold smooth-transition relative group">
                    <i class="fas fa-tools <%= language === 'ar' ? 'ml-2' : 'mr-2' %>"></i>
                    <%= language === 'ar' ? 'أدوات مجانية' : 'Free Tools' %>
                    <div class="absolute -bottom-1 left-0 w-0 h-0.5 bg-gold group-hover:w-full smooth-transition"></div>
                </a>
                <a href="#pricing" class="text-gold/80 hover:text-gold smooth-transition relative group">
                    <i class="fas fa-tags <%= language === 'ar' ? 'ml-2' : 'mr-2' %>"></i>
                    <%= language === 'ar' ? 'الاشتراكات' : 'Pricing' %>
                    <div class="absolute -bottom-1 left-0 w-0 h-0.5 bg-gold group-hover:w-full smooth-transition"></div>
                </a>
                <a href="#contact" class="text-gold/80 hover:text-gold smooth-transition relative group">
                    <i class="fas fa-envelope <%= language === 'ar' ? 'ml-2' : 'mr-2' %>"></i>
                    <%= language === 'ar' ? 'التواصل' : 'Contact' %>
                    <div class="absolute -bottom-1 left-0 w-0 h-0.5 bg-gold group-hover:w-full smooth-transition"></div>
                </a>
            </div>
        </nav>

        <!-- Mobile Menu -->
        <div id="mobile-menu" class="hidden md:hidden border-t border-gold/20 py-4">
            <div class="flex flex-col space-y-3">
                <a href="#home" class="px-4 py-2 text-gold/80 hover:text-gold hover:bg-gold/10 rounded-lg smooth-transition">
                    <i class="fas fa-home <%= language === 'ar' ? 'ml-2' : 'mr-2' %>"></i>
                    <%= language === 'ar' ? 'الرئيسية' : 'Home' %>
                </a>
                <a href="#features" class="px-4 py-2 text-gold/80 hover:text-gold hover:bg-gold/10 rounded-lg smooth-transition">
                    <i class="fas fa-star <%= language === 'ar' ? 'ml-2' : 'mr-2' %>"></i>
                    <%= language === 'ar' ? 'مميزات النظام' : 'Features' %>
                </a>
                <a href="#analysis-types" class="px-4 py-2 text-gold/80 hover:text-gold hover:bg-gold/10 rounded-lg smooth-transition">
                    <i class="fas fa-chart-line <%= language === 'ar' ? 'ml-2' : 'mr-2' %>"></i>
                    <%= language === 'ar' ? 'أنواع التحليل' : 'Analysis Types' %>
                </a>
                <a href="#free-tools" class="px-4 py-2 text-gold/80 hover:text-gold hover:bg-gold/10 rounded-lg smooth-transition">
                    <i class="fas fa-tools <%= language === 'ar' ? 'ml-2' : 'mr-2' %>"></i>
                    <%= language === 'ar' ? 'أدوات مجانية' : 'Free Tools' %>
                </a>
                <a href="#pricing" class="px-4 py-2 text-gold/80 hover:text-gold hover:bg-gold/10 rounded-lg smooth-transition">
                    <i class="fas fa-tags <%= language === 'ar' ? 'ml-2' : 'mr-2' %>"></i>
                    <%= language === 'ar' ? 'الاشتراكات' : 'Pricing' %>
                </a>
            </div>
        </div>
    </div>
</header>

<!-- Hero Section -->
<section id="home" class="relative min-h-screen flex items-center justify-center pt-32 pb-16 overflow-hidden">
    <div class="container mx-auto px-4 text-center relative z-10">
        <!-- Large Interactive Logo -->
        <div class="mb-12">
            <div class="w-48 h-48 mx-auto mb-8 glow-effect rounded-full flex items-center justify-center bg-gradient-to-br from-gold/20 to-gold/5 border border-gold/30 hover:scale-110 smooth-transition cursor-pointer group">
                <img src="/images/logo.png" alt="FinClick.AI" class="w-40 h-40 object-contain pulse-gold group-hover:animate-spin" style="animation-duration: 2s;">
            </div>
            <div class="space-y-4">
                <h1 class="text-5xl md:text-7xl font-bold text-gold mb-6 leading-tight hover:scale-105 smooth-transition cursor-pointer group text-gradient hover:animate-pulse">
                    FinClick.AI
                </h1>
                <p class="text-xl md:text-2xl text-gold/80 mb-8 max-w-4xl mx-auto">
                    <%= language === 'ar' ? 'منصة التحليل المالي الذكية والثورية - ثورة ونقلة نوعية في عالم التحليل المالي' : 'Revolutionary Intelligent Financial Analysis Platform - A Revolution in Financial Analysis' %>
                </p>
                <div class="flex flex-col md:flex-row gap-4 justify-center items-center mt-8">
                    <a href="/register" class="px-8 py-4 bg-gold text-black text-lg font-bold rounded-xl hover:bg-yellow-400 smooth-transition glow-effect pulse-gold">
                        <%= language === 'ar' ? 'ابدأ التحليل الآن' : 'Start Analysis Now' %>
                    </a>
                    <a href="#features" class="px-8 py-4 border border-gold text-gold text-lg font-medium rounded-xl hover:bg-gold/10 smooth-transition">
                        <%= language === 'ar' ? 'اكتشف المميزات' : 'Discover Features' %>
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Animated Background Elements -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none">
        <div class="absolute top-1/4 left-10 w-2 h-2 bg-gold/30 rounded-full animate-ping"></div>
        <div class="absolute top-1/3 right-20 w-1 h-1 bg-gold/20 rounded-full animate-pulse"></div>
        <div class="absolute bottom-1/4 left-1/4 w-3 h-3 border border-gold/20 rounded-full animate-spin" style="animation-duration: 8s;"></div>
        <div class="absolute bottom-1/3 right-1/3 w-2 h-2 bg-gold/10 rounded-full animate-bounce" style="animation-delay: 2s;"></div>
    </div>
</section>

<!-- Main Content: System Features -->
<section id="features" class="py-20 relative">
    <div class="container mx-auto px-4">
        <!-- Section Header -->
        <div class="text-center mb-16">
            <h2 class="text-4xl md:text-5xl font-bold text-gold mb-6">
                <%= language === 'ar' ? 'لماذا FinClick.AI؟' : 'Why FinClick.AI?' %>
            </h2>
            <p class="text-xl text-gold/70 max-w-4xl mx-auto">
                <%= language === 'ar' ? 'ثورة ونقلة نوعية في عالم التحليل المالي - نظام يقلب الدنيا رأساً على عقب ويقلب كل الموازين' : 'A revolution and qualitative leap in the world of financial analysis - a system that turns the world upside down' %>
            </p>
        </div>

        <!-- Features Grid -->
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
            <!-- Feature 1 -->
            <div class="bg-black/50 border border-gold/20 rounded-2xl p-8 hover-glow smooth-transition group">
                <div class="w-16 h-16 bg-gradient-to-br from-gold to-yellow-400 rounded-2xl flex items-center justify-center mb-6 glow-effect group-hover:scale-110 smooth-transition">
                    <i class="fas fa-brain text-black text-2xl"></i>
                </div>
                <h3 class="text-xl font-bold text-gold mb-4">
                    <%= language === 'ar' ? 'يقوم على الذكاء الاصطناعي' : 'AI-Powered Analysis' %>
                </h3>
                <p class="text-gold/70">
                    <%= language === 'ar' ? 'نظام متطور يستخدم أحدث تقنيات الذكاء الاصطناعي لتقديم تحليل مالي دقيق وشامل' : 'Advanced system using cutting-edge AI technologies for accurate and comprehensive financial analysis' %>
                </p>
            </div>

            <!-- Feature 2 -->
            <div class="bg-black/50 border border-gold/20 rounded-2xl p-8 hover-glow smooth-transition group">
                <div class="w-16 h-16 bg-gradient-to-br from-gold to-yellow-400 rounded-2xl flex items-center justify-center mb-6 glow-effect group-hover:scale-110 smooth-transition">
                    <i class="fas fa-chart-line text-black text-2xl"></i>
                </div>
                <h3 class="text-xl font-bold text-gold mb-4">
                    <%= language === 'ar' ? '180+ تحليل مالي' : '180+ Financial Analyses' %>
                </h3>
                <p class="text-gold/70">
                    <%= language === 'ar' ? 'يقدم جميع أنواع التحليل المالي المعروفة في العالم من الكلاسيكية إلى المعقدة جداً' : 'Provides all known types of financial analysis in the world, from classical to highly complex' %>
                </p>
            </div>

            <!-- Feature 3 -->
            <div class="bg-black/50 border border-gold/20 rounded-2xl p-8 hover-glow smooth-transition group">
                <div class="w-16 h-16 bg-gradient-to-br from-gold to-yellow-400 rounded-2xl flex items-center justify-center mb-6 glow-effect group-hover:scale-110 smooth-transition">
                    <i class="fas fa-clock text-black text-2xl"></i>
                </div>
                <h3 class="text-xl font-bold text-gold mb-4">
                    <%= language === 'ar' ? 'السرعة الفائقة' : 'Lightning Speed' %>
                </h3>
                <p class="text-gold/70">
                    <%= language === 'ar' ? 'احصل على التحليل المالي الشامل في ثوانٍ معدودة بضغطة زر واحدة' : 'Get comprehensive financial analysis in seconds with just one click' %>
                </p>
            </div>

            <!-- Feature 4 -->
            <div class="bg-black/50 border border-gold/20 rounded-2xl p-8 hover-glow smooth-transition group">
                <div class="w-16 h-16 bg-gradient-to-br from-gold to-yellow-400 rounded-2xl flex items-center justify-center mb-6 glow-effect group-hover:scale-110 smooth-transition">
                    <i class="fas fa-shield-alt text-black text-2xl"></i>
                </div>
                <h3 class="text-xl font-bold text-gold mb-4">
                    <%= language === 'ar' ? 'دقة عالية (99%)' : 'High Accuracy (99%)' %>
                </h3>
                <p class="text-gold/70">
                    <%= language === 'ar' ? 'دقة وكفاءة متناهية تصل إلى 99% في جميع أنواع التحليلات المالية' : 'Unparalleled accuracy and efficiency reaching 99% in all types of financial analyses' %>
                </p>
            </div>

            <!-- Feature 5 -->
            <div class="bg-black/50 border border-gold/20 rounded-2xl p-8 hover-glow smooth-transition group">
                <div class="w-16 h-16 bg-gradient-to-br from-gold to-yellow-400 rounded-2xl flex items-center justify-center mb-6 glow-effect group-hover:scale-110 smooth-transition">
                    <i class="fas fa-users text-black text-2xl"></i>
                </div>
                <h3 class="text-xl font-bold text-gold mb-4">
                    <%= language === 'ar' ? 'يخدم جميع المستفيدين' : 'Serves All Users' %>
                </h3>
                <p class="text-gold/70">
                    <%= language === 'ar' ? 'يخدم كل مستفيدي التحليل المالي: أفراد، مؤسسات، شركات، منظمات' : 'Serves all financial analysis beneficiaries: individuals, institutions, companies, organizations' %>
                </p>
            </div>

            <!-- Feature 6 -->
            <div class="bg-black/50 border border-gold/20 rounded-2xl p-8 hover-glow smooth-transition group">
                <div class="w-16 h-16 bg-gradient-to-br from-gold to-yellow-400 rounded-2xl flex items-center justify-center mb-6 glow-effect group-hover:scale-110 smooth-transition">
                    <i class="fas fa-cloud text-black text-2xl"></i>
                </div>
                <h3 class="text-xl font-bold text-gold mb-4">
                    <%= language === 'ar' ? 'بيئة سحابية' : 'Cloud-Based Platform' %>
                </h3>
                <p class="text-gold/70">
                    <%= language === 'ar' ? 'استخدم النظام من أي مكان وأي وقت على أي متصفح وأي جهاز' : 'Use the system from anywhere, anytime, on any browser and any device' %>
                </p>
            </div>

            <!-- Feature 7 -->
            <div class="bg-black/50 border border-gold/20 rounded-2xl p-8 hover-glow smooth-transition group">
                <div class="w-16 h-16 bg-gradient-to-br from-gold to-yellow-400 rounded-2xl flex items-center justify-center mb-6 glow-effect group-hover:scale-110 smooth-transition">
                    <i class="fas fa-mouse-pointer text-black text-2xl"></i>
                </div>
                <h3 class="text-xl font-bold text-gold mb-4">
                    <%= language === 'ar' ? 'السهولة الفائقة' : 'Ultimate Simplicity' %>
                </h3>
                <p class="text-gold/70">
                    <%= language === 'ar' ? '3 خطوات فقط: أرفق قوائمك - حدد خيارات التحليل - اضغط زر التحليل' : 'Only 3 steps: Upload statements - Select analysis options - Click analyze' %>
                </p>
            </div>

            <!-- Feature 8 -->
            <div class="bg-black/50 border border-gold/20 rounded-2xl p-8 hover-glow smooth-transition group">
                <div class="w-16 h-16 bg-gradient-to-br from-gold to-yellow-400 rounded-2xl flex items-center justify-center mb-6 glow-effect group-hover:scale-110 smooth-transition">
                    <i class="fas fa-file-alt text-black text-2xl"></i>
                </div>
                <h3 class="text-xl font-bold text-gold mb-4">
                    <%= language === 'ar' ? 'تقارير تفصيلية' : 'Detailed Reports' %>
                </h3>
                <p class="text-gold/70">
                    <%= language === 'ar' ? 'تقارير شاملة وعروض تقديمية احترافية جاهزة للاستخدام' : 'Comprehensive reports and professional presentations ready for use' %>
                </p>
            </div>

            <!-- Feature 9 -->
            <div class="bg-black/50 border border-gold/20 rounded-2xl p-8 hover-glow smooth-transition group">
                <div class="w-16 h-16 bg-gradient-to-br from-gold to-yellow-400 rounded-2xl flex items-center justify-center mb-6 glow-effect group-hover:scale-110 smooth-transition">
                    <i class="fas fa-globe text-black text-2xl"></i>
                </div>
                <h3 class="text-xl font-bold text-gold mb-4">
                    <%= language === 'ar' ? 'مقارنات عالمية' : 'Global Comparisons' %>
                </h3>
                <p class="text-gold/70">
                    <%= language === 'ar' ? 'مقارنات على جميع مستويات العالم وجودة عالمية في التحليل' : 'Comparisons at all global levels with world-class analysis quality' %>
                </p>
            </div>
        </div>

        <!-- Additional Features Text -->
        <div class="mt-16 text-center">
            <div class="max-w-4xl mx-auto space-y-6">
                <p class="text-lg text-gold/80">
                    <%= language === 'ar' ? '"ثورة يعجز أمامها جميع متخصصين وخبراء التحليل المالي العالميين"' : '"A revolution that amazes all global financial analysis specialists and experts"' %>
                </p>
                <p class="text-lg text-gold/80">
                    <%= language === 'ar' ? 'تقديم موقع إلكتروني يعمل كمنصة ونظام كامل شامل يغنيك عن أي مدير أو محلل أو خبير مالي' : 'Providing a website that works as a complete comprehensive platform and system that eliminates the need for any financial manager, analyst, or expert' %>
                </p>
                <p class="text-lg text-gold/80">
                    <%= language === 'ar' ? 'محلل مالي خارق يساعد كل شخص يتخذ القرارات المالية اللحظية' : 'A super financial analyst that helps everyone make instant financial decisions' %>
                </p>
            </div>
        </div>
    </div>
</section>

<!-- Beneficiaries and Purposes Section -->
<%- include('../partials/beneficiaries-section') %>

<!-- System Steps Section -->
<%- include('../partials/steps-section') %>

<!-- Analysis Types Section -->
<section id="analysis-types" class="py-20 bg-gradient-to-b from-gray-900/50 to-black relative">
    <div class="container mx-auto px-4">
        <!-- Section Header -->
        <div class="text-center mb-16">
            <h2 class="text-4xl md:text-5xl font-bold text-gold mb-6">
                <%= language === 'ar' ? 'أنواع التحليل' : 'Analysis Types' %>
            </h2>
            <p class="text-xl text-gold/70 max-w-4xl mx-auto">
                <%= language === 'ar' ? '180+ نوع من التحليل المالي مقسمة إلى تصنيفات أساسية وفرعية لتغطي جميع احتياجات التحليل المالي' : '180+ types of financial analysis divided into basic and sub-classifications to cover all financial analysis needs' %>
            </p>
        </div>

        <!-- Analysis Categories -->
        <div class="grid lg:grid-cols-3 gap-8">
            <!-- Classical Analysis -->
            <div class="bg-black/50 border border-gold/20 rounded-2xl p-8 hover-glow smooth-transition">
                <div class="text-center mb-6">
                    <div class="w-20 h-20 bg-gradient-to-br from-green-500 to-green-700 rounded-2xl flex items-center justify-center mx-auto mb-4 glow-effect">
                        <i class="fas fa-chart-bar text-white text-2xl"></i>
                    </div>
                    <h3 class="text-2xl font-bold text-gold mb-2">
                        <%= language === 'ar' ? 'التحليل الكلاسيكي' : 'Classical Analysis' %>
                    </h3>
                    <div class="text-3xl font-bold text-green-400 mb-4">106 <%= language === 'ar' ? 'تحليل' : 'Analyses' %></div>
                </div>

                <div class="space-y-4 max-h-64 overflow-y-auto">
                    <!-- Liquidity Ratios -->
                    <div class="border border-gold/10 rounded-lg p-4">
                        <h4 class="font-semibold text-gold mb-2">
                            <%= language === 'ar' ? 'نسب السيولة (15 تحليل)' : 'Liquidity Ratios (15 analyses)' %>
                        </h4>
                        <div class="text-sm text-gold/70 space-y-1">
                            <p>• <%= language === 'ar' ? 'نسبة التداول' : 'Current Ratio' %></p>
                            <p>• <%= language === 'ar' ? 'نسبة السيولة السريعة' : 'Quick Ratio' %></p>
                            <p>• <%= language === 'ar' ? 'نسبة السيولة النقدية' : 'Cash Ratio' %></p>
                            <p>• <%= language === 'ar' ? 'نسبة رأس المال العامل' : 'Working Capital Ratio' %></p>
                            <p>• <%= language === 'ar' ? 'نسبة الأصول المتداولة' : 'Current Assets Ratio' %></p>
                        </div>
                    </div>

                    <!-- Profitability Ratios -->
                    <div class="border border-gold/10 rounded-lg p-4">
                        <h4 class="font-semibold text-gold mb-2">
                            <%= language === 'ar' ? 'نسب الربحية (18 تحليل)' : 'Profitability Ratios (18 analyses)' %>
                        </h4>
                        <div class="text-sm text-gold/70 space-y-1">
                            <p>• <%= language === 'ar' ? 'هامش الربح الإجمالي' : 'Gross Profit Margin' %></p>
                            <p>• <%= language === 'ar' ? 'هامش الربح التشغيلي' : 'Operating Profit Margin' %></p>
                            <p>• <%= language === 'ar' ? 'هامش الربح الصافي' : 'Net Profit Margin' %></p>
                            <p>• <%= language === 'ar' ? 'العائد على الأصول' : 'Return on Assets' %></p>
                            <p>• <%= language === 'ar' ? 'العائد على الاستثمار' : 'Return on Investment' %></p>
                        </div>
                    </div>

                    <!-- Activity Ratios -->
                    <div class="border border-gold/10 rounded-lg p-4">
                        <h4 class="font-semibold text-gold mb-2">
                            <%= language === 'ar' ? 'نسب النشاط (20 تحليل)' : 'Activity Ratios (20 analyses)' %>
                        </h4>
                        <div class="text-sm text-gold/70 space-y-1">
                            <p>• <%= language === 'ar' ? 'معدل دوران المخزون' : 'Inventory Turnover' %></p>
                            <p>• <%= language === 'ar' ? 'معدل دوران الذمم' : 'Receivables Turnover' %></p>
                            <p>• <%= language === 'ar' ? 'معدل دوران الأصول' : 'Asset Turnover' %></p>
                            <p>• <%= language === 'ar' ? 'فترة التحصيل' : 'Collection Period' %></p>
                            <p>• <%= language === 'ar' ? 'فترة السداد' : 'Payment Period' %></p>
                        </div>
                    </div>

                    <!-- Leverage Ratios -->
                    <div class="border border-gold/10 rounded-lg p-4">
                        <h4 class="font-semibold text-gold mb-2">
                            <%= language === 'ar' ? 'نسب الرافعة المالية (25 تحليل)' : 'Leverage Ratios (25 analyses)' %>
                        </h4>
                        <div class="text-sm text-gold/70 space-y-1">
                            <p>• <%= language === 'ar' ? 'نسبة الدين إلى حقوق الملكية' : 'Debt to Equity' %></p>
                            <p>• <%= language === 'ar' ? 'نسبة الدين إلى الأصول' : 'Debt to Assets' %></p>
                            <p>• <%= language === 'ar' ? 'نسبة تغطية الفوائد' : 'Interest Coverage' %></p>
                            <p>• <%= language === 'ar' ? 'نسبة الرافعة المالية' : 'Financial Leverage' %></p>
                            <p>• <%= language === 'ar' ? 'نسبة التمويل الذاتي' : 'Equity Financing' %></p>
                        </div>
                    </div>

                    <!-- Market Ratios -->
                    <div class="border border-gold/10 rounded-lg p-4">
                        <h4 class="font-semibold text-gold mb-2">
                            <%= language === 'ar' ? 'نسب السوق (28 تحليل)' : 'Market Ratios (28 analyses)' %>
                        </h4>
                        <div class="text-sm text-gold/70 space-y-1">
                            <p>• <%= language === 'ar' ? 'نسبة السعر إلى الأرباح' : 'Price to Earnings' %></p>
                            <p>• <%= language === 'ar' ? 'نسبة السعر إلى القيمة الدفترية' : 'Price to Book' %></p>
                            <p>• <%= language === 'ar' ? 'نسبة السعر إلى المبيعات' : 'Price to Sales' %></p>
                            <p>• <%= language === 'ar' ? 'عائد السهم' : 'Earnings Per Share' %></p>
                            <p>• <%= language === 'ar' ? 'توزيعات الأرباح' : 'Dividend Yield' %></p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Intermediate Analysis -->
            <div class="bg-black/50 border border-gold/20 rounded-2xl p-8 hover-glow smooth-transition">
                <div class="text-center mb-6">
                    <div class="w-20 h-20 bg-gradient-to-br from-blue-500 to-blue-700 rounded-2xl flex items-center justify-center mx-auto mb-4 glow-effect">
                        <i class="fas fa-chart-line text-white text-2xl"></i>
                    </div>
                    <h3 class="text-2xl font-bold text-gold mb-2">
                        <%= language === 'ar' ? 'التحليل المتوسط' : 'Intermediate Analysis' %>
                    </h3>
                    <div class="text-3xl font-bold text-blue-400 mb-4">21 <%= language === 'ar' ? 'تحليل' : 'Analyses' %></div>
                </div>

                <div class="space-y-4 max-h-64 overflow-y-auto">
                    <!-- Trend Analysis -->
                    <div class="border border-gold/10 rounded-lg p-4">
                        <h4 class="font-semibold text-gold mb-2">
                            <%= language === 'ar' ? 'التحليل الاتجاهي (8 تحليلات)' : 'Trend Analysis (8 analyses)' %>
                        </h4>
                        <div class="text-sm text-gold/70 space-y-1">
                            <p>• <%= language === 'ar' ? 'تحليل الاتجاه الزمني' : 'Time Trend Analysis' %></p>
                            <p>• <%= language === 'ar' ? 'التحليل الأفقي' : 'Horizontal Analysis' %></p>
                            <p>• <%= language === 'ar' ? 'التحليل العمودي' : 'Vertical Analysis' %></p>
                            <p>• <%= language === 'ar' ? 'تحليل النمو' : 'Growth Analysis' %></p>
                        </div>
                    </div>

                    <!-- Comparative Analysis -->
                    <div class="border border-gold/10 rounded-lg p-4">
                        <h4 class="font-semibold text-gold mb-2">
                            <%= language === 'ar' ? 'التحليل المقارن (7 تحليلات)' : 'Comparative Analysis (7 analyses)' %>
                        </h4>
                        <div class="text-sm text-gold/70 space-y-1">
                            <p>• <%= language === 'ar' ? 'مقارنة القطاع' : 'Industry Comparison' %></p>
                            <p>• <%= language === 'ar' ? 'مقارنة المنافسين' : 'Competitor Comparison' %></p>
                            <p>• <%= language === 'ar' ? 'مقارنة الأداء التاريخي' : 'Historical Performance' %></p>
                            <p>• <%= language === 'ar' ? 'معايير الصناعة' : 'Industry Benchmarks' %></p>
                        </div>
                    </div>

                    <!-- Cash Flow Analysis -->
                    <div class="border border-gold/10 rounded-lg p-4">
                        <h4 class="font-semibold text-gold mb-2">
                            <%= language === 'ar' ? 'تحليل التدفقات النقدية (6 تحليلات)' : 'Cash Flow Analysis (6 analyses)' %>
                        </h4>
                        <div class="text-sm text-gold/70 space-y-1">
                            <p>• <%= language === 'ar' ? 'التدفق النقدي التشغيلي' : 'Operating Cash Flow' %></p>
                            <p>• <%= language === 'ar' ? 'التدفق النقدي الحر' : 'Free Cash Flow' %></p>
                            <p>• <%= language === 'ar' ? 'نسب التدفق النقدي' : 'Cash Flow Ratios' %></p>
                            <p>• <%= language === 'ar' ? 'جودة الأرباح' : 'Earnings Quality' %></p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Advanced Complex Analysis -->
            <div class="bg-black/50 border border-gold/20 rounded-2xl p-8 hover-glow smooth-transition">
                <div class="text-center mb-6">
                    <div class="w-20 h-20 bg-gradient-to-br from-red-500 to-red-700 rounded-2xl flex items-center justify-center mx-auto mb-4 glow-effect">
                        <i class="fas fa-brain text-white text-2xl"></i>
                    </div>
                    <h3 class="text-2xl font-bold text-gold mb-2">
                        <%= language === 'ar' ? 'التحليل المتقدم والمعقد' : 'Advanced Complex Analysis' %>
                    </h3>
                    <div class="text-3xl font-bold text-red-400 mb-4">53 <%= language === 'ar' ? 'تحليل' : 'Analyses' %></div>
                </div>

                <div class="space-y-4 max-h-64 overflow-y-auto">
                    <!-- Valuation Models -->
                    <div class="border border-gold/10 rounded-lg p-4">
                        <h4 class="font-semibold text-gold mb-2">
                            <%= language === 'ar' ? 'نماذج التقييم (15 نموذج)' : 'Valuation Models (15 models)' %>
                        </h4>
                        <div class="text-sm text-gold/70 space-y-1">
                            <p>• <%= language === 'ar' ? 'نموذج التدفقات النقدية المخصومة' : 'DCF Model' %></p>
                            <p>• <%= language === 'ar' ? 'نموذج جوردون للنمو' : 'Gordon Growth Model' %></p>
                            <p>• <%= language === 'ar' ? 'تحليل الشركات المشابهة' : 'Comparable Company Analysis' %></p>
                            <p>• <%= language === 'ar' ? 'التقييم بالمضاعفات' : 'Multiple Valuation' %></p>
                        </div>
                    </div>

                    <!-- Risk Analysis -->
                    <div class="border border-gold/10 rounded-lg p-4">
                        <h4 class="font-semibold text-gold mb-2">
                            <%= language === 'ar' ? 'تحليل المخاطر (12 تحليل)' : 'Risk Analysis (12 analyses)' %>
                        </h4>
                        <div class="text-sm text-gold/70 space-y-1">
                            <p>• <%= language === 'ar' ? 'قيمة المخاطر (VaR)' : 'Value at Risk (VaR)' %></p>
                            <p>• <%= language === 'ar' ? 'مخاطر الائتمان' : 'Credit Risk' %></p>
                            <p>• <%= language === 'ar' ? 'مخاطر السوق' : 'Market Risk' %></p>
                            <p>• <%= language === 'ar' ? 'مخاطر السيولة' : 'Liquidity Risk' %></p>
                        </div>
                    </div>

                    <!-- AI & ML Models -->
                    <div class="border border-gold/10 rounded-lg p-4">
                        <h4 class="font-semibold text-gold mb-2">
                            <%= language === 'ar' ? 'النمذجة والمحاكاة (11 نموذج)' : 'Modeling & Simulation (11 models)' %>
                        </h4>
                        <div class="text-sm text-gold/70 space-y-1">
                            <p>• <%= language === 'ar' ? 'تحليل مونت كارلو' : 'Monte Carlo Analysis' %></p>
                            <p>• <%= language === 'ar' ? 'تحليل السيناريوهات المتقدم' : 'Advanced Scenario Analysis' %></p>
                            <p>• <%= language === 'ar' ? 'النماذج التنبؤية' : 'Predictive Models' %></p>
                            <p>• <%= language === 'ar' ? 'التحليل الكمي' : 'Quantitative Analysis' %></p>
                        </div>
                    </div>

                    <!-- Statistical Analysis -->
                    <div class="border border-gold/10 rounded-lg p-4">
                        <h4 class="font-semibold text-gold mb-2">
                            <%= language === 'ar' ? 'التحليل الإحصائي والكمي (15 تحليل)' : 'Statistical & Quantitative Analysis (15 analyses)' %>
                        </h4>
                        <div class="text-sm text-gold/70 space-y-1">
                            <p>• <%= language === 'ar' ? 'نماذج ARIMA و GARCH' : 'ARIMA & GARCH Models' %></p>
                            <p>• <%= language === 'ar' ? 'تحليل السلاسل الزمنية المتقدم' : 'Advanced Time Series Analysis' %></p>
                            <p>• <%= language === 'ar' ? 'التحليل العاملي' : 'Factor Analysis' %></p>
                            <p>• <%= language === 'ar' ? 'الانحدار المتعدد' : 'Multiple Regression' %></p>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- View All Button -->
        <div class="text-center mt-12">
            <button onclick="showAllAnalysisTypes()" class="px-8 py-4 bg-gold text-black text-lg font-bold rounded-xl hover:bg-yellow-400 smooth-transition glow-effect">
                <%= language === 'ar' ? 'عرض جميع الأنواع (180+)' : 'View All Types (180+)' %>
            </button>
        </div>
    </div>
</section>

<!-- Include Free Tools Section -->
<%- include('../partials/free-tools-section') %>

<!-- Include Testimonials Section -->
<%- include('../partials/testimonials-section') %>

<!-- Include Pricing Section -->
<%- include('../partials/pricing-section') %>

<!-- Include Footer -->
<%- include('../partials/footer') %>

<style>
@keyframes scroll {
    0% { transform: translateX(100%); }
    100% { transform: translateX(-100%); }
}

.animate-scroll {
    animation: scroll 60s linear infinite;
}

/* Mobile responsiveness */
@media (max-width: 768px) {
    .animate-scroll {
        animation-duration: 30s;
    }
}

/* Language toggle functionality */
function toggleLanguage() {
    const currentUrl = window.location.href;
    const urlParams = new URLSearchParams(window.location.search);
    const currentLang = urlParams.get('lang') || 'en';
    const newLang = currentLang === 'ar' ? 'en' : 'ar';

    urlParams.set('lang', newLang);
    window.location.href = `${window.location.pathname}?${urlParams.toString()}`;
}

// Mobile menu toggle
document.getElementById('mobile-menu-toggle').addEventListener('click', function() {
    const mobileMenu = document.getElementById('mobile-menu');
    mobileMenu.classList.toggle('hidden');
});

// Add interactive effects for better user experience
document.addEventListener('DOMContentLoaded', function() {
    console.log('🎉 FinClick.AI Platform Loaded Successfully!');
    console.log('📊 180 Financial Analysis Types Ready');
    console.log('💎 Developed by: Razan Ahmed Tawfik');

    // Add parallax effect to neural background
    window.addEventListener('scroll', function() {
        const scrolled = window.pageYOffset;
        const parallax = document.querySelector('.neural-bg');
        if (parallax) {
            parallax.style.transform = `translateY(${scrolled * 0.2}px)`;
        }
    });

    // Add hover effects to buttons and cards
    const interactiveElements = document.querySelectorAll('a[href], button, .hover-glow');
    interactiveElements.forEach(element => {
        element.addEventListener('mouseenter', function() {
            this.style.transform = 'translateY(-2px)';
            this.style.transition = 'all 0.3s ease';
        });

        element.addEventListener('mouseleave', function() {
            this.style.transform = 'translateY(0)';
        });
    });

    // Add click effect to hero logo and title
    const heroLogo = document.querySelector('.w-48.h-48');
    const heroTitle = document.querySelector('h1');

    if (heroLogo) {
        heroLogo.addEventListener('click', function() {
            // Create sparkle effect
            for (let i = 0; i < 12; i++) {
                setTimeout(() => {
                    const sparkle = document.createElement('div');
                    sparkle.style.position = 'absolute';
                    sparkle.style.width = '4px';
                    sparkle.style.height = '4px';
                    sparkle.style.backgroundColor = '#B48500';
                    sparkle.style.borderRadius = '50%';
                    sparkle.style.pointerEvents = 'none';
                    sparkle.style.zIndex = '1000';

                    const rect = this.getBoundingClientRect();
                    sparkle.style.left = (rect.left + rect.width/2) + 'px';
                    sparkle.style.top = (rect.top + rect.height/2) + 'px';

                    document.body.appendChild(sparkle);

                    const angle = (i * 30) * Math.PI / 180;
                    const distance = 100;

                    sparkle.animate([
                        { transform: 'translate(0, 0) scale(1)', opacity: 1 },
                        { transform: `translate(${Math.cos(angle) * distance}px, ${Math.sin(angle) * distance}px) scale(0)`, opacity: 0 }
                    ], {
                        duration: 800,
                        easing: 'ease-out'
                    }).onfinish = () => sparkle.remove();
                }, i * 50);
            }
        });
    }

    // Add click effect to hero title
    if (heroTitle) {
        heroTitle.addEventListener('click', function() {
            // Create glowing text effect
            this.style.textShadow = '0 0 20px #B48500, 0 0 40px #B48500, 0 0 60px #B48500';
            this.style.transform = 'scale(1.05)';

            // Reset effect after animation
            setTimeout(() => {
                this.style.textShadow = '';
                this.style.transform = '';
            }, 800);

            // Create floating letters effect
            const letters = this.textContent.split('');
            const originalText = this.textContent;
            this.innerHTML = '';

            letters.forEach((letter, index) => {
                const span = document.createElement('span');
                span.textContent = letter;
                span.style.display = 'inline-block';
                span.style.transition = 'all 0.5s ease';
                this.appendChild(span);

                setTimeout(() => {
                    span.style.transform = `translateY(-${Math.random() * 20 + 10}px) rotate(${Math.random() * 20 - 10}deg)`;
                    span.style.color = '#FFD700';
                }, index * 50);

                setTimeout(() => {
                    span.style.transform = '';
                    span.style.color = '';
                }, 1000 + index * 50);
            });

            // Restore original text after animation
            setTimeout(() => {
                this.innerHTML = originalText;
            }, 2000);
        });

        // Add hover effect to title
        heroTitle.addEventListener('mouseenter', function() {
            this.style.textShadow = '0 0 10px #B48500';
        });

        heroTitle.addEventListener('mouseleave', function() {
            this.style.textShadow = '';
        });
    }

    // Add smooth scroll for navigation links
    document.querySelectorAll('a[href^="#"]').forEach(link => {
        link.addEventListener('click', function(e) {
            e.preventDefault();
            const targetId = this.getAttribute('href');
            const targetElement = document.querySelector(targetId);
            if (targetElement) {
                targetElement.scrollIntoView({
                    behavior: 'smooth',
                    block: 'start'
                });
            }
        });
    });
});
</style>